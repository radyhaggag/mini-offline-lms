import 'package:flutter/material.dart';

/// Centralized color constants for the app.
/// Use these instead of raw hex codes or `Colors.*` references.
abstract class AppColors {
  /// Primary seed color for Material 3 color scheme generation.
  static const seedColor = Color(0xFF1B5E20);
}
